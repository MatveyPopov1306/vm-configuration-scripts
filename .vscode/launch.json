{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "bashdb",
            // "trace": true,
            "request": "launch",
            "name": "Bash-Debug (simplest configuration)",
            "program": "${file}",
            "args": [
            // "--skip-update",
            // "--restore-sshd-config",
            // "--sshport", "10122",
            "--username", "admin", 
            "--userpassword", "admin",
            // "--skip-fail2ban-setup",
            // "--skip-ssh-key-setup",
            "--ssh-publickey", ""
            ],
            "terminalKind": "integrated",
            //"preLaunchTask": "Clear Terminal"
        }
    ]
}
