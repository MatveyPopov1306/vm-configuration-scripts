{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "bashdb",
            "trace": true,
            "request": "launch",
            "name": "Bash-Debug (simplest configuration)",
            "program": "${file}",
            "args": [
            "--skip-update", "--username", "admin", "--userpassword", "admin"
            ],
            "terminalKind": "integrated",
            //"preLaunchTask": "Clear Terminal"
        }
    ]
}